# ---------- BACKEND ----------
backend:
  name: git-gateway
  repo: liammenu/Liammenu
  branch: main
  identity_url: https://auth.decapbridge.com/sites/f73a5bd9-5d5a-456d-88ce-5fc9e282b117
  gateway_url: https://gateway.decapbridge.com
  commit_messages:
    create: 'Crear {{collection}}: {{slug}}'
    update: 'Actualizar {{collection}}: {{slug}}'
    delete: 'Eliminar {{collection}}: {{slug}}'
    uploadMedia: 'Subir imagen: {{path}}'
    deleteMedia: 'Eliminar imagen: {{path}}'

# ---------- CONFIGURACIÓN GENERAL ----------
media_folder: "images"
public_folder: "/images"
locale: es
show_preview_links: false
display_url: https://liammenu.github.io/Liammenu

# ---------- COLECCIONES (ESPAÑOL) ----------
collections:

  # ---------- OFERTAS ----------
  - name: "ofertas"
    label: "Ofertas"
    label_singular: "Oferta"
    folder: "data/ofertas"
    format: json
    extension: json
    create: true
    slug: "{{slug}}"
    identifier_field: title
    summary: "{{title}} – ${{price}}"
    fields:
      - { label: "Nombre", name: "title", widget: "string", required: true }
      - { label: "Precio", name: "price", widget: "number", value_type: "int", min: 0, required: true }
      - { label: "Imagen", name: "image", widget: "image", required: false }
      - { label: "Descripción", name: "description", widget: "text", required: false }
      - { label: "Categoría", name: "categoria", widget: "hidden", default: "OFERTAS" }
      - { label: "¿Disponible?", name: "disponible", widget: "boolean", default: true }

  # ---------- PIZZAS ----------
  - name: "pizzas"
    label: "Pizzas"
    label_singular: "Pizza"
    folder: "data/pizzas"
    format: json
    extension: json
    create: true
    slug: "{{slug}}"
    identifier_field: title
    summary: "{{title}} – ${{price}}"
    fields:
      - { label: "Nombre", name: "title", widget: "string", required: true }
      - { label: "Precio", name: "price", widget: "number", value_type: "int", min: 0, required: true }
      - { label: "Imagen", name: "image", widget: "image", required: false }
      - { label: "Descripción", name: "description", widget: "text", required: false }
      - { label: "Categoría", name: "categoria", widget: "hidden", default: "PIZZAS" }
      - { label: "¿Disponible?", name: "disponible", widget: "boolean", default: true }

  # ---------- LOMOS ----------
  - name: "lomos"
    label: "Lomos"
    label_singular: "Lomo"
    folder: "data/lomos"
    format: json
    extension: json
    create: true
    slug: "{{slug}}"
    identifier_field: title
    summary: "{{title}} – ${{price}}"
    fields:
      - { label: "Nombre", name: "title", widget: "string", required: true }
      - { label: "Precio", name: "price", widget: "number", value_type: "int", min: 0, required: true }
      - { label: "Imagen", name: "image", widget: "image", required: false }
      - { label: "Descripción", name: "description", widget: "text", required: false }
      - { label: "Categoría", name: "categoria", widget: "hidden", default: "LOMOS" }
      - { label: "¿Disponible?", name: "disponible", widget: "boolean", default: true }

  # ---------- MILANESAS ----------
  - name: "milanesas"
    label: "Milanesas"
    label_singular: "Milanesa"
    folder: "data/milanesas"
    format: json
    extension: json
    create: true
    slug: "{{slug}}"
    identifier_field: title
    summary: "{{title}} – ${{price}}"
    fields:
      - { label: "Nombre", name: "title", widget: "string", required: true }
      - { label: "Precio", name: "price", widget: "number", value_type: "int", min: 0, required: true }
      - { label: "Imagen", name: "image", widget: "image", required: false }
      - { label: "Descripción", name: "description", widget: "text", required: false }
      - { label: "Categoría", name: "categoria", widget: "hidden", default: "MILANESAS" }
      - { label: "¿Disponible?", name: "disponible", widget: "boolean", default: true }

  # ---------- ZAPPIN Y PAPAS ----------
  - name: "zappin"
    label: "Zappin y Papas"
    label_singular: "Zappin"
    folder: "data/zappin"
    format: json
    extension: json
    create: true
    slug: "{{slug}}"
    identifier_field: title
    summary: "{{title}} – ${{price}}"
    fields:
      - { label: "Nombre", name: "title", widget: "string", required: true }
      - { label: "Precio", name: "price", widget: "number", value_type: "int", min: 0, required: true }
      - { label: "Imagen", name: "image", widget: "image", required: false }
      - { label: "Descripción", name: "description", widget: "text", required: false }
      - { label: "Categoría", name: "categoria", widget: "hidden", default: "ZAPPIN Y PAPAS" }
      - { label: "¿Disponible?", name: "disponible", widget: "boolean", default: true }

  # ---------- PATYS ----------
  - name: "patys"
    label: "Patys"
    label_singular: "Paty"
    folder: "data/patys"
    format: json
    extension: json
    create: true
    slug: "{{slug}}"
    identifier_field: title
    summary: "{{title}} – ${{price}}"
    fields:
      - { label: "Nombre", name: "title", widget: "string", required: true }
      - { label: "Precio", name: "price", widget: "number", value_type: "int", min: 0, required: true }
      - { label: "Imagen", name: "image", widget: "image", required: false }
      - { label: "Descripción", name: "description", widget: "text", required: false }
      - { label: "Categoría", name: "categoria", widget: "hidden", default: "PATYS" }
      - { label: "¿Disponible?", name: "disponible", widget: "boolean", default: true }
