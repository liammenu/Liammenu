backend:
  name: git-gateway
  branch: main # o master dependiendo de tu rama principal
  repo: liammenu/Liammenu # tu usuario/repositorio

media_folder: "assets/images"
public_folder: "/assets/images"

collections:
  - name: "menu"
    label: "Menú"
    folder: "_menu"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    fields:
      - { label: "Título", name: "title", widget: "string" }
      - { label: "Descripción", name: "description", widget: "text" }
      - { label: "Imagen", name: "image", widget: "image" }
      - { label: "Precio", name: "price", widget: "number" }
      - { label: "Categoría", name: "category", widget: "select", options: ["Entradas", "Platos Principales", "Postres", "Bebidas"] }